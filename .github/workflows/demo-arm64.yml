name: arm64

# Own workflow so it gets a dedicated status badge. Manual trigger; pick a
# branch in the Actions UI when running.
on:
  workflow_dispatch:

jobs:
  arm64:
    uses: ./.github/workflows/demo-hosted.yml
    with:
      arch: arm64
      runs-on: ubuntu-24.04-arm
